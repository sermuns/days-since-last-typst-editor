[package]
name = "days-since-last-typst-editor"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/sermuns/days-since-last-typst-editor"

[dependencies]
csv = "1"
maud = "0.27"
serde = { version = "1", features = ["derive"] }
